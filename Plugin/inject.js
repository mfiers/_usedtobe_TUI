$.ajaxSetup ({
    // Disable caching of AJAX responses
    cache: false
});


//tui script to inject blank meta-data tags into the page
//once this is finished it will call $(document).tuiLoaded event
TUI.init();
